---
description: demoproject Figma 还原、设计 token 与共享 UI 组件规则
alwaysApply: false
---

# Figma And UI Implementation Rules

在实现 Figma 页面、Compose 页面、设计 token、共享组件或资源切图时使用。

## Figma 流程

- 开发前先用 Figma MCP metadata 确认目标节点范围。
- 如果 URL 指向 section、symbol、instance 或子图层，先向上定位到完整 screen frame。
- 再读取 design context；尺寸、间距、字体、颜色、圆角、描边、阴影以 MCP 结构化数据为准，截图只作视觉参考。
- 每批页面按 Design Tokens -> Shared Components -> Responsive Layout -> Page State -> Navigation -> Preview/Test 顺序落地。
- 所有图标、图片、矢量、字体等设计资源必须来自 Figma MCP 导出或项目已有正式资源，不用占位图、emoji 或纯色块冒充。
- 读 design context 时同时记录每个图层的 Auto Layout 方向、Fill/Hug/Fixed 约束、min/max 尺寸与 spacing；只抄绝对坐标和宽高的还原一律视为不合格。

## Responsive Baseline

- Figma 画板宽度只是基准（默认 390dp）：数值按 1:1 当 dp 读，但它描述的是"这一档宽度下的表现"，不是最终写死值。
- 禁止的适配手段：改写 `LocalDensity` / `densityDpi` 做全局等比缩放；用 `screenWidthDp / 390f` 算缩放系数；把 Figma 绝对坐标用 `offset` 复刻；给页面或区块写 `height(812.dp)` 这类整屏固定高；把状态栏 / 导航栏高度写成 24dp、48dp 经验值。
- 固定 dp 只允许描述"元素自身物理尺寸"：图标边长、描边、圆角、头像直径、最小触控 48dp、分割线粗细。
- 位置与占比一律交给布局求解：宽度用 `fillMaxWidth()` / `weight()`，间距用 `Arrangement.spacedBy(token)` 与 `padding(token)`，对齐用 `Alignment` / `Arrangement`。
- 竖向内容默认可滚动（`verticalScroll` 或 `LazyColumn`），保证 320dp 宽 / 640dp 高的小屏不截断。

## Figma 约束到 Compose 的映射

| Figma | Compose |
| --- | --- |
| Auto Layout horizontal / vertical | `Row` / `Column` |
| item spacing | `Arrangement.spacedBy(Spacing.x)` |
| padding | `Modifier.padding(...)`（RTL 用 start/end） |
| Fill container | `Modifier.weight(1f)` 或 `fillMaxWidth()` |
| Hug contents | 不加尺寸修饰符，让内容撑开 |
| Fixed W/H | `Modifier.width/height(N.dp)`，仅限图标类元素 |
| Min/Max width | `Modifier.widthIn(min = , max = )` |
| 绝对定位图层 | `Box` + `align()` + `padding`，不用 `offset` 抄坐标 |
| 固定比例图片 / 卡片 | `Modifier.aspectRatio(w / h)` |
| 贴屏幕底部的按钮 | `Scaffold` bottomBar 或 `align(BottomCenter)` + `navigationBarsPadding()` |
| 等分网格 | `LazyVerticalGrid(GridCells.Adaptive(minSize))` |

## Breakpoints And Insets

- 断点统一读 `:ui:foundation` 的 `currentWindowSize()`，返回 `DemoWindowSize`（Compact <600dp / Medium 600-840dp / Expanded >840dp）；真实值由 `MainActivity` 用 `calculateWindowSizeClass` 经 `ProvideWindowSize` 下发，未提供时自动回退 `LocalConfiguration`，`@Preview` 无需额外接线。
- 页面禁止直接读 `LocalConfiguration.screenWidthDp` 或引用 Material 的 `WindowSizeClass`；阈值改动只在 `WindowBreakpoints` 一处。
- Compact 单列；Medium / Expanded 走双列、侧栏，或用 `Modifier.readableContentWidth()` 限宽居中，不要把手机布局横向拉满。横屏、分屏、折叠屏展开归入同一套断点，不额外开分支。
- 组件级自适应（同一卡片在不同容器宽度换排版）用 `BoxWithConstraints` 读 `maxWidth`，不要回头去读屏幕宽度。
- 安全区一律走 `WindowInsets`：`statusBarsPadding()` / `navigationBarsPadding()` / `safeDrawingPadding()` 或 `Scaffold` 给的 `paddingValues`，配合 edge-to-edge。
- 含输入框的页面用 `imePadding()`，并保证内容能滚动到键盘之上。

## Text And RTL

- 字号用 `sp` 与排版 token；禁止改用 `dp` 或关闭系统字体缩放来"保证不换行"。
- 不给 `Text` 固定高度；溢出用 `maxLines` + `TextOverflow.Ellipsis`，或允许换行让容器长高。
- 长文案基线：`values-en` / `values-tr` / `values-es` / `values-pt` / `values-ar` 与 `fontScale = 1.5` 下不裁切、不重叠、不挤压兄弟节点。
- 支持阿拉伯语：只用 `start` / `end`，禁止 `left` / `right`；方向性图标用 `AutoMirrored` 图标或显式镜像。

## 适配验证

- 每个还原页面至少提供：`@PreviewScreenSizes`（或手写 360x640 小屏 + 常规 + 平板）、`@PreviewFontScale`、RTL Preview（`locale = "ar"`）。
- 合入前在小屏、常规机、平板或横屏各跑一次目标页面。
- 走查项：底部按钮不被手势条盖住、键盘不遮输入框、长文案不截断、平板无超宽拉伸控件、RTL 布局正确镜像。

## Token And Components

- 页面开发前盘点 `:ui:foundation`、`:ui:designsystem` 现有 token 与主题（含 `DemoBrandThemes`）。
- 新原子 token 放 `:ui:foundation`，语义 token / 主题 / 可复用视觉控件放 `:ui:designsystem`。
- 当前无独立 `:ui:components` / `:ui:social`；通用 Button/Input/Card/Avatar/Dialog/TopBar 等不要长期私有堆在 feature，应下沉到 `designsystem`（或后续拆出的 components 模块）。
- 页面内避免裸写 `Color(...)`、`.dp`、`.sp`、`RoundedCornerShape(...)`、`Brush...` 等视觉常量。
- 允许保留的私有 Composable 仅限一次性页面结构或强页面语义区块。
- 品牌差异优先走 flavor 资源与 `DemoBrandThemes`，不要复制整页实现。

## Compose Quality

- 页面必须覆盖 loading、empty、error、disabled、long text、keyboard、network failure。
- 长列表使用 stable key、分页、骨架屏、错误重试并关注滚动性能。
- 图片使用 Coil，定义占位、错误图、裁剪与缓存策略。
- 下沉组件提供稳定参数、默认 token 样式、Preview、可访问语义和 `modifier` 扩展入口。
