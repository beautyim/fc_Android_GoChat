---
description: demoproject 多语言、测试、混淆、发布与项目维护规则
alwaysApply: false
---

# Release I18n Testing Rules

在修改用户文案、多语言、测试、Gradle、混淆、发布配置、README 或上线门禁时使用。

## Localization

- 用户可见文案进入 string resource，不在 ViewModel 或页面散落硬编码。
- 上线相关页面维护 `values`、`values-en`、`values-tr`、`values-es`、`values-pt`、`values-ar`（`app` 与各 `product` / `ui` 模块按所属文案放置）。
- 默认跟随系统；应用内语言切换只保存 BCP-47 language tag。
- 阿拉伯语检查 RTL、数字、头像/按钮方向和长文本折行。
- 错误文案面向用户可理解，技术细节放日志，不直接透传后端原始错误。

## Verification

- 合入前至少跑通 Debug 构建（`:app:assembleDebug`）与相关 `testDebugUnitTest`；构建失败时不要继续扩大页面范围。
- 涉及混淆、JNI、RTC、数据库、网络协议或上传时，额外跑 release minify 或说明无法执行原因。
- P0：构建、Billing mapper、订单恢复、Match/MQTT payload、登录态、Store catalog、核心 ViewModel 状态流。
- P1：Repository 错误映射、DTO mapper、Compose UI、Navigation、WorkManager。

## Release And Maintenance

- Release 启用 R8 与资源压缩；共用 `config/common/release-log-stripping.pro`。
- 新增 Retrofit、serialization、Room、SQLCipher、Paho MQTT、Agora、Hilt、反射或 JNI 依赖时同步补 keep/consumer rules。
- `google-services.json`、密钥、渠道、域名按环境隔离；`config/` 的 tracked 文件只放占位，真实密钥在 `*.local.properties`。
- README 保持本地启动、配置、构建命令与能力边界说明；TODO / Fake / placeholder 须有上线前清理清单。
