package com.example.demoproject.platform.network.constants

object NetworkHeaders {
    const val AUTHORIZATION = "Authorization"
    const val BEARER_PREFIX = "Bearer "
    /** User-Agent header name used by the legacy-compatible encryption contract. */
    const val USER_AGENT = "ua"
}
